# Bilimxon Backend - Environment Variables Example
PORT=3001

# DATABASE_PATH handles persistent volume storage paths on Render or Railway.
# If configured (e.g. /data), database.json will be saved here and persisted across deployments.
# DATABASE_PATH=/data
